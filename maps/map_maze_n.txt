for i in mazes/maze512-1-*.map; do printf "$i\n"; ../bin/release/bidirectional -nbs $i ../scenarios/$i.scen | grep "^nodes" | awk 'BEGIN {} {a += $2; b += $3; c += $4; d += $5; e+= $6; f+= $7; g+= $8; s++ } END { printf("s: %d A*: %f NBS: %f NBSlazy: %f NBSgreedy: %f \n",s, a,e,f,g) }'; done
